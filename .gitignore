# ============================================================
# NEVER DEPLOY — alles hier landet NIE im öffentlichen Repo
# ============================================================

# Sensitive business docs
BUDGET-PAKETE-PLAN.txt
MARKETING-PLAN-ADDUS.txt
EBAY-KLEINANZEIGEN.txt
CLAUDE.md.txt
restpro.txt
website-strategie-document.md
skills-lock.json
ler.txt

# AI / Agent / Editor tooling (Claude, Trae, Cursor, Copilot)
.claude/
.trae/
.agents/
.cursor/
.aider*
.copilot/
AGENTS.md
CLAUDE.md

# Bots / Scraper / Automatisierungs-Tools (mit Login-Daten!)
kleinanzeigen-bot/
cloudinary-upload/
powershell
*-bot/
chrome-profile/
**/Login Data*
**/Cookies*

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*.tmp
.vscode/
.idea/

# Dependencies / build artifacts
node_modules/
dist/
build/
*.log

# Secrets / API tokens / Credentials
.env
.env.*
*.key
*.pem
*.pfx
*.p12
*token*
*secret*
*credentials*
*.crt
*.cer
auth.json
service-account*.json

# Worker-Code (Referenz, gehört nicht auf den Webserver)
cloudflare-worker-csp.js

# Projekt-Doku (enthält Account-IDs, gehört nicht öffentlich)
PROJEKT-DOKU.txt

# Backup / temp files
*.bak
*.backup
*.old
*~

# npm artifacts (dev only)
package.json
package-lock.json
node_modules/
# Dead/experimental files
assets/Caveat-Regular.ttf
js/auftragsbuch.js
